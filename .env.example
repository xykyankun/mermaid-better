# ===========================================
# Neon Database Configuration
# ===========================================
# Get these values from your Neon Console: https://console.neon.tech

# Neon Auth URL (public - exposed to browser)
NEXT_PUBLIC_NEON_AUTH_URL=https://<your-project-id>.neonauth.<region>.aws.neon.tech/<database>/auth

# Neon Data API URL (public - exposed to browser)
NEXT_PUBLIC_NEON_DATA_API_URL=https://<your-project-id>.apirest.<region>.aws.neon.tech/<database>/rest/v1

# Database connection URL (server-side only - for Drizzle migrations)
DATABASE_URL=postgresql://<user>:<password>@<your-project-id>-pooler.<region>.aws.neon.tech/<database>?sslmode=require

# ===========================================
# Application Configuration
# ===========================================

# App URL (change in production)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# App name and description (optional - used in metadata)
NEXT_PUBLIC_APP_NAME=My App
NEXT_PUBLIC_APP_DESCRIPTION=A full-stack application powered by Neon
